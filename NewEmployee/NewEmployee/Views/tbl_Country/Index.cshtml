@model IEnumerable<NewEmployee.Models.tbl_Country>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cou_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cou_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cou_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cou_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cou_Id })
        </td>
    </tr>
}

</table>
