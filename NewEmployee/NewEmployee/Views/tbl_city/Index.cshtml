@model IEnumerable<NewEmployee.Models.tbl_city>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cit_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cit_couId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cit_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cit_couId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cit_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cit_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cit_Id })
        </td>
    </tr>
}

</table>
